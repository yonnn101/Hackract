# Report Generator Instrument

Python script to gather all logs and results in the working directory and generate a consolidated Markdown report.

## Usage
`python3 instruments/report_gen/report_gen.py`

## Output
- Generates `REPORT.md` in the current working directory containing contents of all `.txt`, `.json`, `.xml`, `.log` files.
